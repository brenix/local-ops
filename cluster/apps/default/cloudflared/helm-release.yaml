---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: cloudflared
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.5.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    image:
      repository: cloudflare/cloudflared
      tag: 2023.5.0@sha256:20a5410bc564bbdd4330310dcde80898ac2f484c178371d5c92c62b81cdea8ca
    args: ["tunnel", "--no-autoupdate", "run"]
    service:
      main:
        enabled: false
    hostNetwork: true
    env:
      TUNNEL_TOKEN:
        valueFrom:
          secretKeyRef:
            name: cloudflared
            key: token
