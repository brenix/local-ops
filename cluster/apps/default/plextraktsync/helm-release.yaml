---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: plextraktsync-watch
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.1.3
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    image:
      repository: ghcr.io/taxel/plextraktsync
      tag: 0.24.0@sha256:d55fb0b6ec9d4e7125fee12f37295af29cece7f40a36afa5f08f0589fdea5581
    args:
      - watch
    envFrom:
      - secretRef:
          name: plextraktsync
    service:
      main:
        enabled: false
    persistence:
      config:
        enabled: true
        type: configMap
        name: plextraktsync
        mountPath: /app/config/config.yml
        subPath: plextraktsync.yaml
        readOnly: true
      pytrakt:
        enabled: true
        type: secret
        name: plextraktsync-pytrakt
        mountPath: /app/config/.pytrakt.json
        subPath: .pytrakt.json
        readOnly: true
    probes:
      liveness:
        enabled: false
      readiness:
        enabled: false
      startup:
        enabled: false
