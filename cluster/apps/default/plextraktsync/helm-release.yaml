---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: plextraktsync-watch
  namespace: default
spec:
  interval: 15m
  chart:
    spec:
      chart: app-template
      version: 1.0.1
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
  install:
    createNamespace: true
    remediation:
      retries: 5
  upgrade:
    remediation:
      retries: 5
  values:
    image:
      repository: ghcr.io/taxel/plextraktsync
      tag: 0.23.11@sha256:787b9720ec50d4f92441d7a7cf239a4e45b2feb2cd84237bb267d4578cfb2479
    args:
      - watch
    envFrom:
      - secretRef:
          name: plextraktsync
    service:
      main:
        enabled: false
    persistence:
      config:
        enabled: true
        type: configMap
        name: plextraktsync
        mountPath: /app/config/config.yml
        subPath: plextraktsync.yaml
        readOnly: true
      pytrakt:
        enabled: true
        type: secret
        name: plextraktsync-pytrakt
        mountPath: /app/config/.pytrakt.json
        subPath: .pytrakt.json
        readOnly: true
    probes:
      liveness:
        enabled: false
      readiness:
        enabled: false
      startup:
        enabled: false
