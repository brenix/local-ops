apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: system-upgrade-controller
    namespace: flux-system
spec:
    force: true
    interval: 30m
    path: components/system-upgrade-controller
    prune: true
    sourceRef:
        kind: OCIRepository
        name: default
    targetNamespace: system-upgrade
